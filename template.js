const template = "h: m: s: t";

const template = "h: m: s: t"
const clock = template
    .replace("h", "03")
    .replace("m", "33")
    .replace("s", "33")
    .replace("t", "AM")

console.log(clock)
